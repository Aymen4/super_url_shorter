<h1>Create new url_shortoner</h1>

<%= form_for(@shortoner) do |f| %>  
  <div class="field"> 
    <%= f.text_area :url %> 
  </div> 

  <div class="actions"> 
    <%= f.submit "Create" %> 
  </div> 
<% end %>
